/**
 * Loads Vite's ambient browser and static-asset declarations into the TypeScript project.
 * This makes side-effect stylesheet imports resolvable during strict CI type checking.
 */

/// <reference types="vite/client" />

