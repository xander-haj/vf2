/**
 * Preserves the established block catalog import path while generated content owns every record.
 * Numeric IDs and hotbar order now originate in canonical JSON through the content compiler.
 */

export { BLOCK_DEFINITIONS, HOTBAR_BLOCKS } from "../generated/block-registry";
